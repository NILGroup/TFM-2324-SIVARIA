import { View, Text, ActivityIndicator, StyleSheet } from "react-native";

const ProfessionalsQuestionnaireSivariaScreen = () => {
    return(
        <View style={{flex:1, backgroundColor: 'white'}}>
            
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
      margin: 20,
    },
   containerButtonStyle:{
            display: 'flex',
            flexDirection: 'row',
            justifyContent: 'flex-end',
            paddingLeft: 10,
            paddingRight: 30,
          }
  });

export default ProfessionalsQuestionnaireSivariaScreen;